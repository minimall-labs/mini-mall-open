server:
  port: 8092

spring:
  application:
    name: open-bff

minimall:
  gateway:
    base-url: http://127.0.0.1:8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
