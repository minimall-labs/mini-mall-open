openapi: 3.0.3
info:
  title: MiniMall Open API (shell)
  version: 0.1.0
  description: Placeholder OpenAPI spec for ISV integrations.
servers:
  - url: http://127.0.0.1:8092
paths:
  /open/v1/health:
    get:
      summary: Open server health
      responses:
        "200":
          description: OK
  /open/v1/ping:
    get:
      summary: Open server ping
      responses:
        "200":
          description: OK
